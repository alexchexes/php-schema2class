# TODO

Since we're about to allow multiple styles (mutable, immutable, PHP8 readonly properties vs. getters/setters, etc.), it would be nice to provide end-users the `presets` option, to specify one of pre-configured options set.