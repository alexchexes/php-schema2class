{
  "allOf": [
    {
      "type": "object",
      "properties": {
        "street_address": { "type": "string", "description": "Descr for 'street_address'" },
        "house_number": { "type": "string", "description": "Descr for 'house_number'" },
        "type": { "enum": ["residential", "business"], "description": "Descr for 'type'" }
      },
      "required": ["street_address"]
    },
    {
      "properties": {
        "city": { "type": "string", "description": "Descr for 'city'" },
        "state": { "type": "string", "description": "Descr for 'state'" }
      },
      "required": ["state"]
    }
  ]
}
